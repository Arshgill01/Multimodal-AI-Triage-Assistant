Read `AGENTS.md` and `docs/ai/*.md` first.

Do not edit files.

Task:
Produce an implementation plan for the requested feature or fix.

Output format:
1. goal
2. current behavior
3. likely files to inspect
4. exact files to change
5. step-by-step implementation plan
6. validation plan
7. risks / unknowns

Rules:
- be path-specific
- keep the plan grounded in current code
- call out stale docs if relevant
- if the live path differs from a standalone script, say so explicitly
